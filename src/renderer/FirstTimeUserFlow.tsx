import React from 'react';

interface Props {
  onNavigate: (view: any) => void;
  onComplete: () => void;
}

export default function FirstTimeUserFlow(_props: Props): React.ReactElement | null {
  return null;
}
