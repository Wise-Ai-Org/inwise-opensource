{
  "project": "inwise-opensource (parity worktree)",
  "branchName": "ralph/sdk-parity-driver",
  "description": "ACTIVE PRD GUARD: Phase 1 — OSS consumes shared AudioTab. Master PRD: C:\\Users\\Shrav\\Inwise\\desktop-recording-sdk\\PRD\\sdk-parity-audit.md Section 5 Phase 1. SCOPE: US-105 ONLY. Phase 0 (US-001..US-006) and Phase 1 shared stories (US-101..US-104) are pre-requisites and completed; do not re-execute. If progress.txt contains any US-IDs other than US-105, HALT and request progress.txt reset. MODEL: claude-sonnet-4-6. This worktree is at C:\\Users\\Shrav\\Inwise\\inwise-opensource-parity, separate from the main checkout, branched off ralph/phase-0-foundation (so src/shared submodule is already wired). Driver script updates submodule to latest shared main HEAD before launching this run.",
  "userStories": [
    {
      "id": "US-105",
      "title": "Replace OSS audio settings with shared AudioTab",
      "description": "OSS's Settings.tsx currently has an Audio Settings section that uses local components. Replace that section with the shared AudioTab component from @inwise/desktop-shared. Wire the onSelectionChange callback to the existing OSS config store via the existing config:set IPC channel (keys micDeviceId / speakerDeviceId).",
      "acceptanceCriteria": [
        "src/shared submodule is already pointed at latest shared main HEAD (driver script did this — confirm src/shared/src/components/AudioTab/AudioTab.tsx exists and is built into src/shared/dist/)",
        "Read C:\\Users\\Shrav\\Inwise\\inwise-opensource-parity\\src\\renderer\\Settings.tsx — locate the audio-settings section (look for getUserMedia, enumerateDevices, mic-test logic, or section header text 'Audio' or 'Microphone')",
        "Replace the audio-settings section's UI with `<AudioTab initialMicId={config.micDeviceId} initialSpeakerId={config.speakerDeviceId} onSelectionChange={handleAudioChange} />` imported from '@inwise/desktop-shared'",
        "handleAudioChange: on each call, persist micId and speakerId via the existing config:set IPC channel (the same path used to save other config keys). If config.ts does not currently store speakerDeviceId, add the field — it's a string in Config interface, persisted to config.json",
        "Delete the now-unused local audio-test helpers from Settings.tsx ONLY if they have no other callers — grep first for cross-references in src/renderer/",
        "Verify tsconfig.renderer.json has the @inwise/desktop-shared path alias (added in US-005 to tsconfig.main.json — copy to renderer config if missing). Path: 'src/shared/dist/index'",
        "Run `npx tsc -p tsconfig.renderer.json --noEmit` — passes",
        "Run `npm run build` end-to-end in OSS — passes",
        "Commit ALL changes (modified Settings.tsx, possibly modified config.ts/main.ts for new IPC, possibly updated tsconfig.renderer.json) with message: feat: US-105 — OSS Settings audio section uses shared AudioTab",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": "OSS uses tsconfig.renderer.json for renderer-side code (Settings.tsx is a renderer file). Path alias @inwise/desktop-shared was added to tsconfig.main.json in US-005 — likely needs adding to tsconfig.renderer.json too. Do NOT touch main.ts unless adding a new IPC channel for speakerDeviceId. Branched from ralph/phase-0-foundation in worktree — does not block main checkout. Driver script handles submodule pointer commit separately — do not commit src/shared bump as part of this story."
    }
  ]
}
