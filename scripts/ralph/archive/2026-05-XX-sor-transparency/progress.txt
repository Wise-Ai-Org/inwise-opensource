## Codebase Patterns
- Tests are standalone Node.js scripts using `node:assert/strict`; export `run()` and guard with `if (require.main === module)`. Run with `ts-node --project tsconfig.main.json`.
- Typecheck via `node_modules/.bin/tsc -p tsconfig.main.json --noEmit` (no global tsc).
- In-memory NeDB for tests: each DB module exposes `__set<Name>DbForTests(db)` setters. Follow this pattern for new modules.
- Pipeline entry point for transcripts: `createMeetingFromTranscript({ title, content, date })` in `database.ts`, then `extractInsights(content)` + `saveInsights(meetingId, insights)`.
- Injectable deps pattern for testability: pass optional `deps` object with stub functions, default to real implementations.

# Ralph Progress Log
Started: Mon Jun  1 17:19:18 PDT 2026
---

## 2026-06-01 17:50 PDT - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Defined `NormalizedSegment` and `NormalizedTranscript` types in `zoom-transcript-ingestion.ts`
- Implemented `segmentsToText()` pure helper (speaker-prefix lines, one per segment)
- Implemented `ingestNormalizedTranscript()` which calls `createMeetingFromTranscript` → `extractInsights` → `saveInsights`, reusing the recorder's existing pipeline without duplication
- Added injectable `deps.runInsights` for testability (default: real pipeline)
- Added `__setMeetingsDbForTests` to `database.ts` following the existing pattern
- Wrote `zoom-transcript-ingestion.test.ts` covering: pure text conversion, meeting creation in DB, pipeline called with correct content, graceful handling when insights throw
- Installed `ts-node` as dev dependency to run tests
- Files changed: `src/main/zoom-transcript-ingestion.ts` (new), `src/main/zoom-transcript-ingestion.test.ts` (new), `src/main/database.ts` (+4 lines), `package.json`, `package-lock.json`
- **Learnings for future iterations:**
  - The recorder entry point is `createMeetingFromTranscript` in `database.ts` — always reuse this, never duplicate the pipeline
  - `extractInsights` calls the Anthropic API so must be stubbed in tests; wrap in try/catch so meeting persists even without API access
  - `__setMeetingsDbForTests` now exists alongside `__setTasksDbForTests` for full in-memory test coverage
  - Build typecheck: `node_modules/.bin/tsc -p tsconfig.main.json --noEmit`; test runner: `node_modules/.bin/ts-node --project tsconfig.main.json <file>`
---

## 2026-06-01 17:55 PDT - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added `credentialsDb` Datastore to `database.ts`, initialized in `initDatabase()` as `credentials.db`
- Exported `ZoomCredentials` interface with all 5 required fields
- Implemented `getZoomCredentials()`, `setZoomClientCredentials()`, `setZoomTokens()`, `clearZoomCredentials()` with singleton document pattern (`_id: 'zoom'`)
- Added `__setCredentialsDbForTests()` test-injection setter following established pattern
- Created `zoom-credentials.test.ts` covering: null on empty, create via setZoomClientCredentials, upsert/overwrite, token merge (preserving clientId), tokens-only creation, and clearZoomCredentials
- Files changed: `src/main/database.ts` (+71 lines), `src/main/zoom-credentials.test.ts` (new, 77 lines)
- **Learnings for future iterations:**
  - Zoom credentials use singleton document pattern: one doc with `_id: 'zoom'` in `credentials.db`
  - `setZoomTokens` can create the doc even without clientId/clientSecret (for token-refresh scenarios)
  - `__setCredentialsDbForTests` follows the same pattern as `__setMeetingsDbForTests` and `__setTasksDbForTests`
  - All five Zoom fields: `zoomClientId`, `zoomClientSecret`, `zoomAccessToken`, `zoomRefreshToken`, `zoomTokenExpiresAt`
---

## 2026-06-01 18:10 PDT - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created `zoom-oauth.ts` with full OAuth Authorization Code flow: loopback HTTP server on port 17292, code exchange with Basic Auth (Zoom's required method), token storage via US-002 database functions, token refresh, and test-connection helper
- Added 6 IPC handlers in `main.ts`: zoom:saveCredentials, zoom:connect, zoom:disconnect, zoom:status, zoom:test, zoom:redirectUri
- Added 6 preload bridge entries in `preload.ts` following existing Jira pattern
- Added `ZoomSettings` React component to `Settings.tsx`: shows redirect URI prominently (for user to configure in Zoom app), Client ID/Secret inputs, Connect/Disconnect button, connected state
- Files changed: `src/main/zoom-oauth.ts` (new, 154 lines), `src/main/main.ts` (+22 lines), `src/main/preload.ts` (+7 lines), `src/renderer/Settings.tsx` (+80 lines)
- **Learnings for future iterations:**
  - Zoom uses Basic Auth (base64 clientId:clientSecret) for token exchange, unlike Jira which uses PKCE + body params
  - Port 17292 is reserved for Zoom OAuth loopback; Jira uses 17291
  - ZoomSettings is self-contained (own state) — does NOT use the parent Config/update pattern because Zoom creds go to the DB (credentialsDb), not electron-store config
  - getZoomCredentials() returns null if no doc exists; callers must guard for null
  - Token storage uses ISO string for zoomTokenExpiresAt (new Date(...).toISOString()); comparison converts back to epoch via new Date(str).getTime()
---

## 2026-06-01 23:00 PDT - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented parseTimestampMs() to convert VTT HH:MM:SS.mmm timestamps to milliseconds
- Implemented parseZoomVtt() to split VTT into cue blocks, extract speaker-label prefix and text, handle CRLF, empty VTT, and cues without speaker labels (defaulting to 'Unknown')
- Implemented downloadAndParseVtt() with injectable fetchFn dep, downloads VTT with Bearer token, delegates to parseZoomVtt()
- Created 	est-transcripts/zoom-sample.vtt fixture with 4 speaker-labeled cues (2 speakers, varied timestamps)
- Wrote zoom-vtt-parser.test.ts with 8 test cases: timestamp parsing, fixture parse, no-speaker cue, CRLF line endings, empty VTT, colon-in-body, download+parse, HTTP error
- Files changed: src/main/zoom-vtt-parser.ts (new), src/main/zoom-vtt-parser.test.ts (new), 	est-transcripts/zoom-sample.vtt (new)
- **Learnings for future iterations:**
  - VTT blocks split on double newline; find timestamp line by ' --> ', text lines follow after it
  - Zoom speaker format is 'Speaker Name: text' � only split on first ': ' to preserve colons in body text
  - fixture path from src/main test: path.join(__dirname, '../../test-transcripts/zoom-sample.vtt') � __dirname is src/main when ts-node runs
---

## 2026-06-01 23:10 PDT - US-006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added `zoom:listRecordings` IPC handler in `main.ts`: checks connected state first, calls `listZoomRecordings()`, returns structured error for not-connected and API failures
- Added `zoom:fetchTranscript` IPC handler: calls `getTranscriptDownloadUrl()` → `downloadAndParseVtt()` → `ingestNormalizedTranscript()`, returns structured errors for no-transcript and fetch failures; token refresh is handled transparently by `getValidZoomToken`
- Added `zoomListRecordings` and `zoomFetchTranscript` bridge entries to `preload.ts`
- Extended `ZoomSettings` React component in `Settings.tsx`: when connected, shows "Fetch from Zoom" button; listing/ready/fetching/done/error states surface clear messages; recording picker lists recent meetings with title and date; success shows meeting title; all failure paths name the cause
- Added `ZoomRecordingItem` interface and `FetchFlowState` type alias to renderer for type safety
- Files changed: `src/main/main.ts` (+26 lines), `src/main/preload.ts` (+2 lines), `src/renderer/Settings.tsx` (+122 lines)
- **Learnings for future iterations:**
  - IPC handler pattern for multi-step flows: check preconditions first (connected?), return `{ ok: false, error }` at each step rather than throwing — keeps renderer error handling uniform
  - `getValidZoomToken` (called inside `listZoomRecordings` / `getTranscriptDownloadUrl`) handles token refresh transparently; callers do not need to check expiry
  - Frontend fetch flow state machine: idle → listing → ready → fetching → done/error; always expose a "reset" path so users can retry without refreshing
  - `zoom:fetchTranscript` passes the full recording object from the listing step (meetingId, uuid, title, startedAt); `uuid` is used for the recordings endpoint, `meetingId` and metadata for ingestion
---
