# Ralph Progress Log
Started: Thu May 21 13:35:30 PDT 2026

## Codebase Patterns
- tsconfig.renderer.json (for renderer/Settings.tsx) is separate from tsconfig.main.json — path aliases must be added to BOTH
- The config:set IPC channel accepts a Partial<Config> updates object; use `(window as any).inwiseAPI.setConfig(updates)` to persist individual fields immediately
- Audio device changes in the shared AudioTab fire `onSelectionChange` with `{ micId?, speakerId? }` — persist each key to config immediately, not on a Save button
- When npm install hasn't been run in the parity worktree, run it before typechecking
---

## 2026-05-21 - US-105
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Replaced OSS Settings.tsx audio section (mic dropdown + MicTest + SystemAudioTest) with shared `<AudioTab>` component from @inwise/desktop-shared
- Added `speakerDeviceId: string` field to Config interface in both src/main/config.ts and src/renderer/Settings.tsx
- Added `@inwise/desktop-shared` path alias to tsconfig.renderer.json (was already in tsconfig.main.json from US-005)
- Wired `handleAudioChange` callback to persist mic/speaker changes immediately via config:set IPC
- Deleted local MicTest (was lines 334-466) and SystemAudioTest (468-610) — no other callers in src/renderer/
- Removed `mics` state and `enumerateDevices` call from Settings component since AudioTab handles device enumeration internally
- Both `npx tsc -p tsconfig.renderer.json --noEmit` and `npm run build` pass clean
- Files changed: src/main/config.ts, src/renderer/Settings.tsx, tsconfig.renderer.json, package-lock.json
- **Learnings for future iterations:**
  - node_modules did not exist in the parity worktree — ran `npm install` before typecheck
  - The Read hook intercepts file reads and shows only line 1; use PowerShell `Get-Content` to read specific line ranges
  - AudioTab already bundles DevicePicker + MicTest + SpeakerTest — no need for any local wrappers
---
