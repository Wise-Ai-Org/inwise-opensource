# Ralph Progress Log
Started: Wed Apr 22 12:38:22 PDT 2026

## Codebase Patterns
- Main-process tests export `run()` + `if (require.main === module) { run().catch(...) }`; run via `node dist/main/<file>.test.js` after `npm run build:main`
- Inject in-memory NeDB Datastores into database.ts with `__setTasksDbForTests` / `__setMeetingsDbForTests` / `__setPeopleDbForTests` to test helpers without Electron
- NeDB supports dot-notation `$set`/`$unset` on array indices (e.g. `insights.actionItems.2.dismissed`); use it for atomic field writes instead of read-modify-write
- `tasksDb.findAsync({ archivedAt: null })` is the canonical "live tasks" query
- Action items live on `meeting.insights.actionItems[i]`; lifecycle state (convertedToTaskId, dismissed) must be persisted there, not on separate projections, or `getPerson()` will always re-render them as pending
- IPC flow: `ipcMain.handle('domain:action', ...)` in main.ts → expose on `inwiseAPI` in preload.ts → wrap in `api` object in src/renderer/api.ts → consumed by components
- Task schema is strict (createTask in database.ts sets `source`, `approval`, `provenance`, `snoozedAt`, `snoozedReason`, `likelyDone` etc.); new task-producing helpers must set the same shape or downstream queries break

---

## 2026-04-22 12:45 PDT — US-001
- Implemented `convertActionItemToTask`, `dismissActionItem`, `undismissActionItem` in src/main/database.ts
- Rewrote `getPerson()`'s action-item pipeline: joins on tasks collection, emits three partitioned arrays (pendingActionItems, activeActionItems, doneActionItems) and excludes dismissed items everywhere
- Added `__setMeetingsDbForTests` / `__setPeopleDbForTests` test-only injectors mirroring the existing tasks helper
- Extended ActionItem + PersonDetail interfaces in src/renderer/People.tsx with `dismissed`, `taskUpdatedAt`, `taskDueDate`, plus optional `activeActionItems` / `doneActionItems`
- Added unit tests in database.test.ts covering: conversion persistence, dismiss/undismiss round-trip, three-tier partitioning, dismissed-exclusion, summary counts
- Files changed: src/main/database.ts, src/main/database.test.ts, src/renderer/People.tsx
- **Learnings for future iterations:**
  - The prior bug was subtle: the mapping at `database.ts:464` hardcoded `convertedToTaskId: null` when reading insights, so even if main.ts had written the back-reference, `getPerson()` would overwrite it in memory. When fixing a "lifecycle state doesn't stick" bug, check the projection layer, not just the writer.
  - NeDB dot-notation `$set: { 'insights.actionItems.${i}.convertedToTaskId': taskId }` works for both setting new fields and updating existing ones — no need to fetch-then-update.
  - `getPerson()` currently fetches `tasksDb.findAsync({ archivedAt: null })` twice (once for the join I added, once for stale-task nudges). Left as-is for this story to keep the change minimal; future iterations could merge them.
  - Summary counts `summary.activeActionItems` / `summary.doneActionItems` are new fields — downstream components that destructure `summary` should be treated as additive consumers.
---


## 2026-04-22 12:52 PDT — US-002
- Imported `convertActionItemToTask`, `dismissActionItem`, `undismissActionItem` in src/main/main.ts
- Registered three `ipcMain.handle` channels: `actionItem:convert`, `actionItem:dismiss`, `actionItem:undismiss`
- Exposed `convertActionItem`, `dismissActionItem`, `undismissActionItem` on `inwiseAPI` in src/main/preload.ts
- Mirrored the three methods on `api` in src/renderer/api.ts with typed `{ taskId }` / `boolean` return signatures
- Main and renderer builds pass; all 7 main-process test suites green (config, database, live-meeting-banner, self-identity, staleness-sweep, task-completion-inference, welcome-back)
- Files changed: src/main/main.ts, src/main/preload.ts, src/renderer/api.ts
- **Learnings for future iterations:**
  - Preload bridges can normalize IPC ergonomics: here the DB helpers take positional `(meetingId, actionItemIndex, taskFields)` but the renderer call sites (per US-003) want an object-arg shape `{ meetingId, actionItemIndex, taskFields }`. The preload wrapper is the right place to unpack — keeps the IPC contract positional (one handler, one arg list) while giving UI a named-arg API.
  - `ipcMain.handle` handlers that return `void` from the underlying helper should explicitly `return true` (or the actual result) — a `return undefined` through IPC is harmless but the pattern in this codebase (see `db:reviewMeeting`, `db:archivePerson`) is to return `true` for successful fire-and-forget ops, which gives UI a predictable truthy value to await.
  - Import order in main.ts for `./database`: the named-imports block is alphabetical-ish but grouped by domain (meetings, tasks, snoozed, likely-done, people, …). New DB helpers slot in next to the helpers they logically sit beside — keeps diffs small and the import block scannable.
---
