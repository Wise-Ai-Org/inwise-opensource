# Ralph Progress Log
Started: Thu May 21 12:31:56 PDT 2026
---

## Codebase Patterns
- electron-builder bundles node_modules — use `"file:src/shared"` npm dependency for submodule packages so they resolve at runtime, not just via tsconfig paths
- tsconfig.main.json needs both `baseUrl: "."` AND `paths` for path aliases to work with `tsc`
- Two tsconfig files: tsconfig.main.json (main process, commonjs) + tsconfig.renderer.json (renderer, ESNext/bundler)
- No dedicated test runner script in package.json; quality check is `tsc -p tsconfig.main.json --noEmit`

## 2026-05-21 12:38 PDT - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added git submodule `inwise-desktop-shared` at `src/shared/`
- Built submodule (`npm install && npm run build` in src/shared)
- Added `"@inwise/desktop-shared": "file:src/shared"` to root package.json dependencies
- Updated tsconfig.main.json with `baseUrl: "."` and `paths: { "@inwise/desktop-shared": ["src/shared/dist/index"] }`
- Changed main.ts lines 27 and 55 to import from `@inwise/desktop-shared` instead of `./audio-utils` and `./mfcc`
- Deleted src/main/audio-utils.ts and src/main/mfcc.ts
- Typecheck and build pass (8 files changed, 181 insertions, 477 deletions)
- **Learnings for future iterations:**
  - Submodule dist/ must be built inside the submodule before root typecheck; `npm install && npm run build` in src/shared is required
  - Using both file: dependency AND tsconfig paths covers both runtime (Node.js require) and IDE/typecheck (TypeScript paths resolution)
  - `rootDir` constraint does not block `paths` aliases — TypeScript resolves them from the d.ts files, not the source
  - electron-builder `asar: true` with `node_modules/**/*` in files picks up file: symlinks, so runtime resolution works
---
