# Ralph Progress Log
Branch: ralph/slack-integration
---

## Codebase Patterns
- Electron main process uses `electron-store` for all local persistence (`config.ts`) — use `getConfig()` / `setConfig()` for reads/writes
- Build typecheck: `npm run build:main` (tsc -p tsconfig.main.json); no dedicated `typecheck` script
- Config interface in `config.ts` + defaults in the `Store<Config>` constructor must be updated together when adding new fields
- Slack fields already in config: `slackBotToken`, `slackReadChannels`, `slackWriteChannels`, `slackInactivityWindowMin`
- Logger: `log(level, tag, message)` from `./logger`

## 2026-06-01 17:30 - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Implemented local Slack API client in `src/main/slack-client.ts`
- Added Slack config fields to `src/main/config.ts` (slackBotToken, slackReadChannels, slackWriteChannels, slackInactivityWindowMin)
- Files changed: src/main/slack-client.ts (new), src/main/config.ts (Slack fields added)
- **Learnings for future iterations:**
  - slack-client.ts exposes: listChannels(), getChannelHistory(), getThreadReplies(), postMessage(), resolveUser(), validateToken(), isSlackConnected()
  - All Slack API calls go through slackGet()/slackPost() helpers which handle 429 + Retry-After backoff with up to 5 retries
  - User resolution is cached in-memory (userCache Map) for the process lifetime
  - getToken() reads slackBotToken from electron-store; throws if not configured
---

## 2026-06-01 17:35 - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added Slack IPC handlers to main.ts: slack:connect (validates token via validateToken(), saves to electron-store), slack:disconnect, slack:status, slack:listChannels
- Added Slack methods to preload.ts: slackConnect, slackDisconnect, slackStatus, slackListChannels
- Added SlackSettings React component to Settings.tsx with password input, validate/connect flow, connected state display
- Added Slack fields to Config interface in Settings.tsx (slackBotToken, slackReadChannels, slackWriteChannels, slackInactivityWindowMin)
- Wired SlackSettings into Settings page below Jira Integration section
- Files changed: src/main/main.ts, src/main/preload.ts, src/renderer/Settings.tsx, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - IPC pattern: ipcMain.handle in main.ts + ipcRenderer.invoke in preload.ts + (window as any).inwiseAPI.method in renderer
  - SlackSettings is self-contained (no config prop needed — reads/writes via IPC directly, not via the shared setConfig flow)
  - setConfig in main.ts uses Partial<Config> — cast as any to set individual Slack fields without importing full Config type
  - Renderer Config interface in Settings.tsx is a SEPARATE simplified type from the main-process Config in config.ts
---

## 2026-06-01 17:40 - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created src/main/slack-normalizer.ts: normalizeSlackThread(messages, meta) converts a Slack thread to a NormalizedConversation
- Resolves user IDs to display names via resolveUser() (cached in slack-client.ts)
- Formats transcript as "[HH:MM] Name: text" lines — compatible with the existing extractInsights() pipeline
- Title derived from first 80 chars of parent message; attendees = resolved participant names
- SlackThreadMeta carries channelId, channelName, permalink (slack:// deeplink)
- Files changed: src/main/slack-normalizer.ts (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - The local task pipeline consumes a transcript string via extractInsights(transcript) — matching this format is the key
  - NormalizedConversation maps directly to createMeeting() params: title, date, attendees, transcript, source='slack_thread'
  - Use slackMeta field to attach channel/permalink; store in the meeting doc's source or a custom field
  - tsToDate: multiply ts (float string of epoch seconds) by 1000 for JS Date
---

## 2026-06-01 17:45 - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created src/main/slack-ingestion.ts: computeReadyBatches() evaluates threads for quiet-period eligibility
- In-memory Map (channelId -> threadTs -> ThreadState) tracks latestActivityTs and lastProcessed state
- Threads ready when: quiet >= inactivityWindowMin AND (never processed OR got new replies since last process)
- Loose top-level messages (no replies) collected into LooseMessageBatch per channel per poll window
- markThreadProcessed() records latestActivityTs at time of processing to detect future new replies
- Files changed: src/main/slack-ingestion.ts (new), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - log() only accepts 'info' | 'warn' | 'error' — no 'debug' level
  - threadTs === ts means it's a parent message; threadTs !== ts means it's a reply
  - latestReply field on Slack message = ts of most recent reply (from conversations.history)
  - In-memory state is sufficient for debounce; US-005 adds persistence for cursor tracking
---

## 2026-06-01 17:50 - US-005
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created src/main/slack-cursor-store.ts: persistent cursor + dedup using a separate electron-store file ('slack-cursor-state')
- getChannelCursor/setChannelCursor: per-channel `oldest` param for getChannelHistory() to re-fetch from last seen position
- isThreadProcessed/markThreadIngested/removeThreadIngested: thread-level dedup that survives restarts
- advanceCursorToNewest: moves cursor to the newest ts seen in a batch
- Updated slack-ingestion.ts: integrates persistent dedup — skips already-ingested threads, removes from dedup when new replies appear
- markThreadProcessed now persists to cursor store in addition to updating in-memory state
- Files changed: src/main/slack-cursor-store.ts (new), src/main/slack-ingestion.ts (updated), scripts/ralph/prd.json
- **Learnings for future iterations:**
  - Use a separate electron-store instance (new Store({ name: 'slack-cursor-state' })) for operational state vs user config
  - electron-store defaults only apply to top-level keys; spread + reassign when updating nested objects
  - advanceCursorToNewest finds max ts by comparing parseFloat(ts) values
---

## 2026-06-01 17:55 - US-006
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added postWiserNote(channelId, note) to slack-client.ts
- Guards against channels not in slackWriteChannels list; formats note as "*Wiser Note*\n{note}"
- Intentionally has no callers — placeholder entry point per PRD spec
- Files changed: src/main/slack-client.ts, scripts/ralph/prd.json
---

## 2026-06-01 18:00 - US-007
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Expanded SlackSettings component in Settings.tsx with:
  - Read-channels multi-select (checkboxes with channel name labels)
  - Write-channels multi-select (same channel list)
  - Inactivity window numeric input (default 60, range 1-1440)
  - "Load channels" button that calls slackListChannels IPC
  - "Save Slack Settings" button persists via setConfig IPC
  - Settings load from getConfig() on mount, reload correctly across sessions
- Added SlackChannel interface in Settings.tsx
- Files changed: src/renderer/Settings.tsx, scripts/ralph/prd.json
- **Learnings for future iterations:**
  - setConfig IPC in main.ts does partial updates (Partial<Config>) — safe to pass just the changed fields
  - Channels list is loaded on-demand ("Load channels" button) to avoid unnecessary API calls on settings open
  - Webpack compiles Settings.tsx as part of the main App bundle (not a separate entry) — no separate bundle to check
---
