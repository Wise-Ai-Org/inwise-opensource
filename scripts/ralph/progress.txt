# Ralph Progress Log
Started: Tue Apr 21 11:44:25 PDT 2026
---

## Codebase Patterns
- Config is a single electron-store instance in `src/main/config.ts`. `Config` interface doubles as the schema; `defaults` must list every field (electron-store will not materialize missing fields).
- `getConfig()` returns the whole store snapshot; `setConfig(partial)` writes per-key. Prefer narrow, named helpers (e.g. `addCalendar`, `setSelfEmails`) over dumping partials through `setConfig` so call sites stay readable.
- App start is a single `app.whenReady().then(() => { initDatabase(); ...; calendarWatcher.start(); })` block in `src/main/main.ts`. One-time migrations belong immediately after `initDatabase()` and before any consumer (watcher, windows) reads the migrated fields.
- Logging in main uses the local `log(level, message, detail?)` helper from `src/main/logger.ts` — don't introduce `console.*` in main. Levels are limited to `'info' | 'warn' | 'error'` (no `'debug'`).
- Main typecheck: `npx tsc -p tsconfig.main.json --noEmit`. Renderer typecheck: `npx tsc -p tsconfig.renderer.json --noEmit`. Project has no configured test runner, so "tests pass" means the typechecks stay green.
- Deprecated-but-kept fields: annotate with `/** @deprecated */` on the interface and leave the default in place so existing stores still load; only the migration path should read them.

## 2026-04-21 11:50 PDT - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Extended `Config` in `src/main/config.ts` with `calendars: CalendarSubscription[]` and `selfEmails: string[]`, both defaulting to `[]`. Kept `googleIcsUrl`/`outlookIcsUrl` as `@deprecated` fields (migration still reads them).
- Exported `CalendarProvider` and `CalendarSubscription` types plus helpers: `listCalendars`, `addCalendar`, `updateCalendar`, `removeCalendar`, `setSelfEmails`.
- Added `migrateLegacyCalendars()`: early-returns when `calendars.length > 0` (idempotent guard) and otherwise seeds one row per non-empty legacy URL using `crypto.randomUUID()` for ids and labels `'Google'`/`'Outlook'`.
- Wired the migration into `app.whenReady()` in `src/main/main.ts` right after `initDatabase()` and before `createMainWindow()`/`calendarWatcher.start()`; logs `config:migrate-calendars` at `info` level only when rows were actually added.
- Files changed: `src/main/config.ts`, `src/main/main.ts`.
- Quality: both `tsc --noEmit` targets pass; no test runner exists in the project.
- **Learnings for future iterations:**
  - `electron-store` stores on disk are persistent across runs, so the `calendars.length > 0` guard is the migration's only safety net — don't "clean up" the deprecated fields in the same pass, or a user who rolls back to a pre-US-001 build loses their subscriptions.
  - Consumers reading calendar URLs (currently `src/main/calendar-watcher.ts`) still need to be migrated to iterate `getConfig().calendars` — that is the work US-002 picks up. Until then, calendars[] is written but nothing reads it at runtime.
  - `randomUUID` lives in Node's `crypto` module in this Electron main process — no extra dep needed.
  - The PRD phrased the helpers module as "new `src/main/calendar-config.ts` (or extend config.ts)". I chose to extend `config.ts` because the surface is small (four helpers) and splitting would fragment the single `Store<Config>` instance across files.
---

## 2026-04-21 11:55 PDT - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Rewrote `CalendarWatcher.poll()` in `src/main/calendar-watcher.ts` to iterate `listCalendars().filter(c => c.enabled && c.url.trim())` instead of reading the deprecated `googleIcsUrl`/`outlookIcsUrl` directly.
- Switched from sequential `for...of` fetch to `Promise.allSettled` so one slow/failed calendar can't stall the others.
- Added `sourceCalendarId: string` to the `CalendarEvent` shape; every event coming out of `fetchIcsEvents()` carries the calendar it came from (test path passes `'__test__'`).
- Replaced the simple `id`-only dedup with a composite key `${id}|${startTime.toISOString()}` Map. On collision, the entry with more attendees wins; ties keep the first-fetched (Map insertion order).
- Per-calendar log line on each fetch: `calendar-watcher:fetch | calendarId=X label="Y" got=N events`.
- Aggregate poll line: `calendar-watcher:poll | Done — total=M unique events across K calendars`.
- Empty-enabled-list short-circuits with an `info` log (logger.ts only knows info/warn/error, not debug — see Codebase Patterns).
- Health rollup now: status=`ok` when at least one calendar succeeded, `error` only when every calendar failed; `lastError` populated whenever any failure happens. `googleConfigured`/`outlookConfigured` are derived from `calendars[]` rather than legacy fields, so the existing `Settings.tsx` health card keeps compiling until US-004 swaps it out.
- Notifications (`meeting-starting`/`meeting-reminder`) now fire from the *deduped* list, not the raw merged list — otherwise a meeting present on two synced calendars would notify twice.
- Files changed: `src/main/calendar-watcher.ts`.
- Quality: both `tsc --noEmit` targets pass. No test runner.
- **Learnings for future iterations:**
  - `logger.ts` only accepts `'info' | 'warn' | 'error'` — there is no `'debug'` level. The PRD's "skip polling with a debug log" got translated to `info` to keep typecheck green; if a debug level is wanted, add it to `logger.ts` first.
  - `CalendarHealth.googleConfigured`/`outlookConfigured` are now derived, not authoritative. `Settings.tsx` still consumes them; US-004 should remove both fields and the consumer in the same commit so the deprecation window is short.
  - Composite-key dedup matters: the previous `id`-only dedup would have silently dropped one calendar's copy of a cross-shared event without preserving attendee context. The Map-based "prefer more attendees" rule only kicks in when the same event lands twice — no behavior change for single-calendar users.
  - `Promise.allSettled` returns results in input order, so we can pair `results[i]` with `enabled[i]` for per-calendar error attribution without threading the calendar id through the rejection.
  - `fetchIcsEvents()` now requires a `calendarId` argument; `testUrl()` passes the sentinel `'__test__'` because Settings → Test isn't tied to a saved row yet (US-004 will change that).
---

## 2026-04-21 12:00 PDT - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Added `src/main/self-identity.ts` exporting `isSelf(attendee)` (reads `getConfig().selfEmails` + `userName`) and a pure `matchesSelf(attendee, selfEmails, userName)` helper kept side-effect-free so tests can run without loading electron-store. Rule: lowercased `attendee.includes()` any non-empty self-email, OR `attendee.includes()` a non-empty trimmed `userName`. Handles `Name <email>`, plain email, and plain display-name attendees.
- Empty-config (`selfEmails=[]` and blank `userName`) returns `false` — preserves fresh-install behavior where nothing is filtered from attendee lists.
- Replaced inline filters:
  - `src/main/main.ts` — `replaceSpeakerLabels`'s `otherAttendees` now uses `attendees.filter(a => !isSelf(a))`; the `userName` local survives because it's still used to label speaker 0.
  - `src/main/main.ts` — `autoEnrollVoices` `otherAttendees` uses `!isSelf(a)`. The early-return on blank `userName` stays (it has a different purpose: no known name to enroll as).
  - `src/main/database.ts` — in the `getPerson` action-item filter, replaced the `(currentUserName && (owner.includes(currentUserName) || currentUserName.includes(owner)))` disjunct with `isSelf(owner)` and dropped the now-unused `const currentUserName` read.
  - `src/main/database.ts` — in `getSuggestedPeople`, dropped the `userName` const and replaced the `if (userName && key === userName) continue` equality check with `if (isSelf(attendee)) continue` placed *before* lowercasing so aliases never reach the frequency map.
- Added `src/main/self-identity.test.ts` using `node:assert/strict` with cases: email-only, display-name fallback, `Name <email>` combined, mixed-case (both sides), multi-email (one match), no-match, empty config, whitespace-only email/userName ignored, and empty-attendee. Verified green by compiling to a temp dir and running with node (output: `self-identity: all tests passed`). No permanent test runner was wired — `src/main/self-identity.test.ts` is picked up by `tsconfig.main.json` for typecheck coverage but ships in `dist/` as an unused module.
- Files changed: `src/main/self-identity.ts` (new), `src/main/self-identity.test.ts` (new), `src/main/main.ts`, `src/main/database.ts`.
- Quality: both `tsc --noEmit` targets green.
- **Learnings for future iterations:**
  - `tsconfig.main.json` has `include: ["src/main/**/*"]` and no test-file exclusion, so any `*.test.ts` in `src/main/` will be built into `dist/main/`. Harmless for a single leaf test module, but if the suite grows, either move tests to a sibling `tests/` dir with its own tsconfig or add a `**/*.test.ts` exclude pattern — excluding will drop them from typecheck too, so you'd need a paired `tsconfig.test.json` for that.
  - Splitting `isSelf` into a pure `matchesSelf(attendee, selfEmails, userName)` + a thin `isSelf` that pulls from `getConfig()` is the pragmatic way to get unit tests without mocking `electron-store`. Follow this shape for any future identity/config-coupled predicates.
  - Direction matters for fuzzy name matches. The old `database.ts:395` code did *bidirectional* `includes()` (`owner.includes(userName) || userName.includes(owner)`). `isSelf` is *unidirectional* (`owner.includes(userName)` only). The loosening was acceptable per the PRD and reduces false positives, but any caller expecting the old "userName is a substring of owner" behavior will regress silently.
  - In `getSuggestedPeople`, the self-check must run on the raw `attendee` string *before* lowercasing to the map key. Placing the guard after `const key = attendee.toLowerCase()` still works but wastes the allocation; more importantly, keeping the guard first makes it obvious the self path is a short-circuit, not a tail-end filter.
  - `autoEnrollVoices`'s `const userName = config.userName?.trim()` survived because the early-return is semantically about "can we label speaker 0 at all", not about self-filtering. Don't collapse that into `isSelf` — they answer different questions.
---

## 2026-04-21 12:05 PDT - US-004
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Replaced the two fixed `IcsField` components in `src/renderer/Settings.tsx` with a `CalendarList` + `CalendarRowView` pair. Removed `IcsField` entirely.
- Each row: label input (debounced 500ms + save-on-blur), provider `<select>` (Google/Outlook/Other → 'google'/'outlook'/'ics'), URL input (debounced+blur), "Enabled" checkbox (saves immediately), Test, Delete.
- Test wires to existing `inwiseAPI.testCalendarUrl` — the event-count/error message renders inline directly under the URL row.
- `+ Add calendar` appends a blank row server-side via `inwiseAPI.addCalendar({ label:'', provider:'ics', url:'', enabled:true })`; the returned `id` is tracked in a `newlyAddedId` state that the child row consumes to auto-focus the label input on mount.
- Empty state: dashed-border placeholder with copy "Add your first calendar to get started — paste a secret ICS link from your calendar settings."
- `CalendarStatus` left untouched — the aggregation it already showed (ok/error/no-url + `eventCount` + `lastError`) is already "total event count + any-failing flag" per the PRD; `lastError` is populated whenever any single calendar fails even if others succeed (see calendar-watcher health rollup).
- Delete uses `window.confirm` with explicit warning that events from that calendar will disappear from the upcoming list.
- Added three new IPC handlers in `main.ts` — `calendar:add`, `calendar:update`, `calendar:remove` (plus `calendar:list`) — each calls `calendarWatcher.refresh()` so toggling a calendar off removes its events on the NEXT poll cycle within seconds, not at the 5-minute interval.
- Added a public `refresh()` method on `CalendarWatcher` that awaits `poll()`.
- `src/main/preload.ts` exposes `listCalendars`, `addCalendar`, `updateCalendar`, `removeCalendar`.
- Removed `googleIcsUrl` and `outlookIcsUrl` from the renderer `Config` interface — they stay in the backend store (used by the migration path only), but the Settings form no longer reads/writes them.
- Files changed: `src/main/calendar-watcher.ts`, `src/main/main.ts`, `src/main/preload.ts`, `src/renderer/Settings.tsx`.
- Quality: both `tsc --noEmit` targets pass; `npm run build:renderer` (webpack) compiles successfully.
- Browser verification: not applicable — this is an Electron app whose renderer runs inside a BrowserWindow, not a standalone web page the `dev-browser` skill can navigate to. Typecheck + webpack build are the real signals here; interactive testing needs `npm start`.
- **Learnings for future iterations:**
  - Because we post to main via IPC for every calendar mutation, the calendar list state lives primarily in the main store (`electron-store`) — the renderer mirrors it with optimistic updates. Don't try to round-trip through the existing `config:get`/`config:set` path: the `calendars[]` array is large/nested enough that per-row `updateCalendar(id, patch)` is much cleaner than serializing the whole array through setConfig.
  - `calendarWatcher.refresh()` is now load-bearing for "toggle off and events disappear". If you add other mutation paths (e.g. bulk import from a file), call `refresh()` there too — otherwise users wait up to 5 minutes for the poll to reflect their change.
  - The renderer's `Config` type is *not* the same shape as `src/main/config.ts`'s `Config`. The renderer uses a narrow subset — don't synchronize them; instead, add fields only if the UI needs them. US-005 will add `selfEmails` to the renderer type for the chip editor.
  - The legacy `googleIcsUrl`/`outlookIcsUrl` fields stay in the backend Config (marked `@deprecated`) so `migrateLegacyCalendars()` can still seed calendars[] for users upgrading from pre-US-001 builds. The renderer does not need to know they exist, and removing them from the renderer Config keeps us from accidentally wiping them via `setConfig(config)` on Save.
  - Per-row auto-focus needs a *ref passed to the input*, set in an effect that fires when `autoFocus` becomes true for that row. Don't try to focus inside the parent's add handler — React hasn't mounted the new row yet at that point.
---
