# Ralph Progress Log
Started: Tue Apr 21 13:19:47 PDT 2026

## Codebase Patterns
- Tests live next to source as `X.test.ts` with a `run()` export and `if (require.main === module) run()`. Build via `npx tsc -p tsconfig.main.json`, then run `node dist/main/X.test.js`. No `npm test` script exists.
- `src/main/config.ts` uses electron-store. `electron-store` cannot be imported from compiled JS at arbitrary locations — it must run from within the project's own `dist/main/` so node_modules resolves. Factor pure logic out of store-dependent helpers for unit tests.
- Main-process typecheck: `npx tsc -p tsconfig.main.json --noEmit` (silent = success). Renderer: `npx tsc -p tsconfig.renderer.json --noEmit`.
- `preload.ts` hardcodes an `allowed[]` channel allowlist — every new main→renderer push channel must be added or it's silently dropped.
- Date→epoch-ms is the convention for main→renderer IPC payloads carrying Date objects.
- `mainWindow` hides on close (preventDefault+hide); fallback UI-path checks must verify `mainWindow.isVisible()`, not just `!isDestroyed()`.
- `import { app } from 'electron'` in plain node is safe at load-time (`app` is `undefined`); it only fails when you actually call an `app.*` method. This means tests can import modules that import electron without booting electron.
- NeDB (`@seald-io/nedb`) without a `filename` option is pure in-memory — perfect for unit tests. `{ field: null }` matches documents where the field is null OR missing (Mongo-compatible), so additive schema changes need no migration.
- For testable DB helpers on `database.ts`, use the `__setTasksDbForTests(db)` injection hook. Follow the same pattern if future stories need to exercise meetings/people DBs in unit tests.

---

## 2026-04-21 13:30 PDT - US-001
- Extended `Config` in `src/main/config.ts` with `lastOpenedAt: string | null` and `welcomeBackLastSeenAt: string | null` (both default null).
- Added helpers: `markAppOpened()`, `getDaysSinceLastOpen()`, `markWelcomeBackSeen()`.
- Factored out pure `computeDaysSince(iso, nowMs)` so the gap-compute logic is testable without spinning up electron-store.
- Snapshotted the stored `lastOpenedAt` at module-load time into `priorLastOpenedAtSnapshot`; `getDaysSinceLastOpen()` reads from the snapshot so downstream code sees the gap from the PREVIOUS session, not the value we just overwrote.
- In `src/main/main.ts`: imported `markAppOpened`, wired it to both `mainWindow.once('ready-to-show', …)` and `mainWindow.on('show', …)` — 'show' fires when the tray menu re-activates a hidden window, so both are required.

Files changed:
- src/main/config.ts
- src/main/main.ts
- src/main/config.test.ts (new)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `node dist/main/config.test.js` — all tests passed.
- `node dist/main/self-identity.test.js` — regression check, all tests still pass.

Learnings for future iterations:
- If a helper's stored-value semantic needs the PRIOR value (not the just-written one), snapshot the store read at module-load time. Future stories (US-004 compute, US-008 suppression) will want the same treatment for any fresh-session signal.
- `markAppOpened` intentionally writes on BOTH `ready-to-show` and `show` — this is double-fire on cold start (once for each event in sequence) but idempotent; writing now-to-now has no effect on getDaysSinceLastOpen since that reads the snapshot.
- `welcomeBackLastSeenAt` has no prior-value snapshot and no read-helper yet — US-004 will compare it directly against `lastOpenedAt` in its null-return branch.
- electron-store v8 requires Electron runtime to resolve paths reliably; this is why the test file only exercises the pure `computeDaysSince` helper and not the store-backed helpers.

---

## 2026-04-21 13:40 PDT - US-002
- Added snooze schema fields to tasks: `snoozedAt`, `snoozedReason`, `lastMentionedAt` (default null) in `createTask` init. Also started stamping `updatedAt` at creation so downstream US-003 eligibility (age since last touch) works for freshly-created tasks too.
- Added helpers in `src/main/database.ts`: `isSnoozed`, `snoozeTask`, `bringBackTask`, `getSnoozedTasks`, `touchLastMentioned`. `snooze`/`bringBack`/`touch*` all bump `updatedAt` per PRD.
- Extracted `sortTasks(tasks[])` so `getTasks` and `getSnoozedTasks` share the priority/createdAt sort.
- `getTasks(opts?: { includeSnoozed?: boolean })` — default excludes snoozed by adding `snoozedAt: null` to the NeDB query (NeDB matches both null and missing, so legacy rows without the field are treated as active). `includeSnoozed: true` drops the filter.
- Added test-only export `__setTasksDbForTests(db)` so unit tests can inject an in-memory NeDB without booting Electron. Production code never uses it.
- Wrote `src/main/database.test.ts` covering all US-002 acceptance gates: isSnoozed purity, createTask field defaults, snooze→excluded, includeSnoozed→all, getSnoozedTasks→only snoozed (with reason + timestamp), bringBack→reappears + reason cleared, touchLastMentioned persists + updatedAt bumps, archived still excluded regardless of includeSnoozed flag.

Files changed:
- src/main/database.ts
- src/main/database.test.ts (new)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `node dist/main/database.test.js` — all tests passed.
- `node dist/main/config.test.js` — regression, all tests passed.
- `node dist/main/self-identity.test.js` — regression, all tests passed.

Learnings for future iterations:
- Electron's module in plain node is a string (path to the binary), so `import { app } from 'electron'` at file-top is NOT a load-time throw — `app` is just `undefined`. Tests can safely import `database.ts` as long as they never call `initDatabase()`; inject a raw `@seald-io/nedb` Datastore instead via the `__setTasksDbForTests` hook.
- `new Datastore()` with no options is pure in-memory — no filesystem, no `app.getPath` — ideal for unit tests. Call `loadDatabaseAsync()` once before use.
- NeDB matches `{ field: null }` against documents where the field is either null OR missing, matching Mongo semantics. This is what makes adding a new nullable field backward-compatible for existing rows without a migration.
- There are still TWO inline `tasksDb.findAsync({ archivedAt: null, ... })` calls in `database.ts` (person-nudges at ~line 504, meeting-agenda at ~line 820) that do NOT filter snoozed. Out of scope for US-002 (PRD scoped to getTasks), but be aware: if snoozed tasks start appearing in person nudges or agenda context in a later iteration, those are the two sites to fix.
- Pattern to follow when adding testable DB helpers to `database.ts`: pure helpers (like `isSnoozed`) stay free-standing; stateful helpers get a corresponding in-memory test using `__setTasksDbForTests`. Follow the same injection pattern for meetings/people DBs if future stories need it.

---

## 2026-04-21 13:45 PDT - US-003
- Added new module `src/main/staleness-sweep.ts` exporting `sweepStaleTasks(now?: Date): Promise<{ snoozed: any[] }>` and `getLastSweepResult(): SweepResult | null`.
- Eligibility implementation follows PRD gates: `status === 'todo'` (this codebase uses 'todo' where the PRD says 'active' — they mean the same thing here), `!isSnoozed(t)`, `updatedAt < now - 30d`, `(!lastMentionedAt || lastMentionedAt < now - 14d)`, `priority !== 'high' && priority !== 'critical'`.
- For each eligible task, call `snoozeTask(id, 'stale-30d')` (reuses US-002's helper — no new DB write path).
- `lastSweepResult` is module-local and includes `{ snoozed, eligible, totalActive, ranAt }` so US-004's compute IPC can read `snoozed.length`, sample titles, and skip re-running if the cached result is fresh.
- Hooked into `app.whenReady()` in `main.ts`: `setTimeout(() => sweepStaleTasks().catch(...), 5_000)` — fire-and-forget, runs after calendar sync starts but before the UI would typically ask for welcome-back data, and is NOT awaited inside whenReady (so startup stays non-blocking per PRD).
- Log line format matches PRD spec: `log('info', 'staleness-sweep', 'snoozed=N eligible=E of total active tasks=T')` — logger formats this as `[ts] INFO staleness-sweep | snoozed=... eligible=... of total active tasks=...`.
- Added test-only `__resetLastSweepResultForTests()` so tests don't leak stashed state across cases.
- Accepts an optional `now: Date` parameter so tests can control the clock deterministically.

Files changed:
- src/main/staleness-sweep.ts (new)
- src/main/staleness-sweep.test.ts (new)
- src/main/main.ts (import + whenReady hook)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `node dist/main/staleness-sweep.test.js` — 8 test blocks passed (happy path, age gate, mention-blocks, mention-too-old-doesnt-block, priority=high + critical, already-snoozed, mixed counts, status=done gate).
- `node dist/main/database.test.js` / `config.test.js` / `self-identity.test.js` — regressions still green.

Learnings for future iterations:
- This codebase's task "active" state is `status === 'todo'`, not literally `'active'`. The PRD used 'active' generically; implementation should treat them as synonymous. If a later story needs to distinguish (e.g., 'in_progress' as a separate active state), update the filter.
- `high` AND `critical` priorities both skip the sweep. PRD only spelled out `priority !== 'high'`, but `critical` is strictly more important than `high`, so excluding it is the obvious-intent interpretation. Documented here so future reviewers don't think it was overreach.
- The sweep does NOT touch `lastMentionedAt` for untouched tasks — it reads it only. US-007 (task completion inference) is where `lastMentionedAt` should start being written by the transcript-processing pipeline.
- `lastSweepResult` is a singleton in module state — fine for Electron main process, but if we ever need per-window isolation or SSR, this needs to move to a store. Not currently a concern.
- The whenReady `setTimeout(5_000)` delay is a soft fence — it is NOT synchronised with `calendarWatcher`'s first `events-updated` emission. If US-008's live-meeting suppression needs the sweep result to be guaranteed-present before rendering, promote the sweep call to listen on `calendarWatcher.once('events-updated', ...)` instead.
- Tests build tasks via `createTask()` then backdate with `updateTask(id, { updatedAt: iso(daysAgo), ... })` because `createTask` stamps `updatedAt` to now. This is the standard pattern for time-sensitive DB tests in this codebase.

---

## 2026-04-21 19:40 PDT - US-004
- Added pure compute module `src/main/welcome-back.ts` exporting `computeWelcomeBack(inputs): WelcomeBackResult | null`. All data dependencies (tasks, meetings, upcoming events, sweep result, config timestamps, openAtLogin) are passed as inputs — zero imports from `electron` or `config` so the function is fully unit-testable.
- Added IPC handlers in `main.ts`:
  - `welcomeBack:compute` — assembles tasks (`getTasks({ includeSnoozed: true })`), meetings (`getMeetings()`), upcoming events (`calendarWatcher.getUpcomingEvents()`), sweep cache (`getLastSweepResult()`), config timestamps, and `app.getLoginItemSettings().openAtLogin`, then calls `computeWelcomeBack`. `getLoginItemSettings` is guarded by try/catch for platforms that throw (Linux dev env).
  - `welcomeBack:dismiss` — calls `markWelcomeBackSeen()`.
- Extended `src/main/config.ts` with `getLastOpenedAtSnapshot()` and `getWelcomeBackLastSeenAt()` so compute can read the pre-session snapshot (the live store value is overwritten on every `show` event by `markAppOpened`).
- Added `welcomeBackCompute` and `welcomeBackDismiss` to `src/main/preload.ts`. Both are invoke-style (no `allowed[]` update needed — that list is only for main→renderer push channels).
- Null-return gates implemented per PRD: (1) `daysSinceLastOpen === null` (first-ever launch), (2) `< 2` days, (3) `welcomeBackLastSeenAt >= lastOpenedAtSnapshot` (already dismissed for this gap). Using the SNAPSHOT, not the live store value, is critical — see `getLastOpenedAtSnapshot` doc comment.
- Ask-selection follows documented priority: contradiction > overdueWithSignal > launchAtStartupOffer. Only the first qualifying ask is returned; when none qualify, `ask` is omitted entirely.
- Contradictions come from `meetings.insights.contradictions` (stored via `saveInsights`), not from re-calling the LLM `detectContradictions` function. Gap filter: `m.createdAt > priorSnapshot`. PRD originally used `detectContradictions.createdAt` but the real source is the meeting row.
- `jiraProgress` = tasks where `source.type === 'jira'` AND `updatedAt > priorSnapshot`; `doneCount` is the subset with `status === 'completed' || 'done'`. No explicit status-change history exists, so `updatedAt` is the best proxy (runDailyJiraPull bumps it on status map).
- `meetingsMatched` counts DISTINCT meeting IDs (via `provenance.meetingId`) for jira-linked tasks whose meeting was created in the gap window. A single meeting with multiple jira-linked tasks counts once.
- Missed meetings for launchAtStartupOffer: calendar events with `startTime` in `(priorOpen, now)` that have NO meetings row, OR only a `status === 'calendar_sync'` placeholder row (calendar sync created a stub but nothing was actually recorded).

Files changed:
- src/main/config.ts (added `getLastOpenedAtSnapshot`, `getWelcomeBackLastSeenAt`)
- src/main/welcome-back.ts (new)
- src/main/welcome-back.test.ts (new)
- src/main/main.ts (imports + two IPC handlers)
- src/main/preload.ts (two new invoke wrappers)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `node dist/main/welcome-back.test.js` — 19 test blocks passed (3 null-return gates + 1 gate counter + 4 wins + 3 ask-priority branches + 6 ask guards + empty-wins sanity + 1 misc).
- `node dist/main/{config,database,staleness-sweep,self-identity}.test.js` — regressions all green.

Learnings for future iterations:
- Pure-function extraction is the key testability pattern in this codebase. `staleness-sweep.ts` does this via `sweepStaleTasks(now?: Date)` (injects the clock); `welcome-back.ts` takes it further and injects ALL dependencies. When a new story needs data from multiple modules (config + db + calendar + staleness), follow this pattern and let `main.ts` be the wiring layer.
- The PRD null-return condition "`welcomeBackLastSeenAt >= lastOpenedAt`" MUST compare against the module-load snapshot from config.ts, NOT the live store value. `markAppOpened()` fires on every `show` event, so the live `lastOpenedAt` is always >= `welcomeBackLastSeenAt` and the gate would never fire. `getLastOpenedAtSnapshot()` is the correct reader. Document this pattern wherever else "pre-session state" is needed.
- Contradictions are stored in `meetings.insights.contradictions` by `saveInsights` — do NOT re-call `detectContradictions` (that's the LLM extractor, it's a COMPUTE function not a STORE). Query `getMeetings()` and filter by `insights.contradictions.length > 0` + createdAt window.
- `getLoginItemSettings` can throw on non-darwin/win32 platforms; wrap in try/catch and default to `false`. US-009 will add the `setLoginItemSettings` side. The PRD's guard ("never return launchAtStartupOffer if already openAtLogin") is in compute, not in the IPC — good for testing, but means the compute also needs a try-safe boolean input.
- `calendarWatcher.getUpcomingEvents()` returns events including the PAST 7 days (see `fetchIcsEvents` starting `todayStart` 7 days back). That's why missed-meetings detection can iterate upcomingEvents rather than needing a separate history query. Document this in AGENTS.md for calendar-watcher if a future story assumes future-only.
- The new preload entries (`welcomeBackCompute`, `welcomeBackDismiss`) are both `invoke` style and do NOT need to be added to the `allowed[]` array in the `on()` handler — that allowlist is ONLY for main→renderer push channels. This distinction is documented in Codebase Patterns as well.
- When adding a new module with `any[]` dependencies (tasks, meetings), default to `any` at the boundary but use well-named interfaces for the OUTPUT shape (WelcomeBackResult, WelcomeBackWins, WelcomeBackAsk). The IPC payload is the contract for US-005's UI.
- Wins fields are individually optional (`?`) — only populated when count > 0. Empty wins + no ask is the signal for US-005's "Nothing urgent while you were out" empty-state copy.
---

## 2026-04-21 19:50 PDT - US-005
- Added `src/renderer/WelcomeBack.tsx`: full-screen overlay that calls `inwiseAPI.welcomeBackCompute()` on mount, renders nothing when the IPC returns null, otherwise renders greeting + wins list + at most one ask + chip row + Done button.
- Wins render count-not-list per PRD copy: cleared, jiraProgress (with doneCount), meetingsMatched, calendarHealthy. Sample titles are NOT shown — PRD says "detail on-demand via chips," so the sampleTitles array coming from compute is intentionally unused by the UI.
- Ask variants: `contradiction` → Review/Dismiss (Review navigates to Communications + dismisses the screen); `overdueWithSignal` → Snooze to next week / Mark done / Keep as-is (all local-dismiss the ask for US-005 scope — US-007 wires completion semantics); `launchAtStartupOffer` → Turn on / Not now (both local-dismiss per PRD note that action logic is deferred to US-009).
- "Nothing urgent" empty-state line renders ONLY when both wins are empty AND no ask is shown (after any local ask-dismiss, the chip row remains so the user isn't trapped).
- Chip row: Tasks → tasks view, Meetings → communications, Jira → settings, Calendar → settings. Clicking any chip calls welcomeBackDismiss() via the shared handleDone() and then navigates.
- Done button top-right calls `inwiseAPI.welcomeBackDismiss()` and unmounts (parent clears `welcomeBackVisible`).
- Integrated into `src/renderer/App.tsx`: new `welcomeBackVisible` state (default true) — WelcomeBack renders while it is true AND FirstTimeUserFlow is not active (avoid stacking modals on first-run).

Files changed:
- src/renderer/WelcomeBack.tsx (new)
- src/renderer/App.tsx (import + conditional render)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `npx webpack --config webpack.renderer.config.js` — bundle compiled successfully.
- `node dist/main/{config,database,staleness-sweep,welcome-back,self-identity}.test.js` — all pass (regression).

Browser verification:
- **Dev-browser skill was NOT available in this session.** No standalone browser test harness exists in this codebase (Electron renderer only bundles via webpack into the app shell). Verification relied on: (a) renderer typecheck, (b) full webpack bundle passes, (c) structural inspection of rendered JSX against PRD copy spec. A follow-up `electron .` run with synthetic `welcomeBack:compute` fixtures is the recommended human smoke test before shipping.

Learnings for future iterations:
- This codebase's UI components use **plain inline styles** for full-screen overlays (see `FirstTimeUserFlow.tsx`) and **Chakra UI** for interactive cards/modals (see `MeetingConflictModal.tsx`). Both are fine; I chose inline styles for `WelcomeBack.tsx` because the overlay pattern is closer to FirstTimeUserFlow and we want minimal dependency coupling.
- Palette: `#0d9488` teal primary, `#14b8a6` teal hover, `#0f172a` ink, `#64748b` muted, `#e2e8f0` border. Matches FirstTimeUserFlow and the Chakra theme.
- `inwiseAPI` is accessed via `(window as any).inwiseAPI` in components — there's `src/renderer/api.ts` that wraps a subset but it doesn't include welcome-back yet. Use the raw `(window as any).inwiseAPI.welcomeBack*` invoke path and handle the missing-API case defensively (for headless test environments, the API object may be absent).
- PRD says "sampleTitles up to 3" but also "NEVER render a list of task/meeting titles (count is the message, detail on-demand via chips)". These are not in conflict — sampleTitles is still useful as telemetry/debug surface in the IPC payload, but the UI MUST NOT display them. I left the data in the compute return and just ignored it in the component.
- There are **only 4 views**: communications, tasks, people, settings. The PRD's chip row mentions "Jira" and "Calendar" — both live inside `settings`. Mapping Jira→settings and Calendar→settings is the pragmatic interpretation.
- The `allowed[]` allowlist in `preload.ts` is for main→renderer **push** channels (`ipcRenderer.on`). New **invoke** handlers (like `welcomeBackCompute`/`welcomeBackDismiss`) do NOT need to be added there. (Reiterated from US-004 for anyone touching preload.)
- When adding new overlays on top of App.tsx, decide stacking order with care: FirstTimeUserFlow is shown to first-run users at a higher z-index (10000) than Sidebar but must take precedence over WelcomeBack. I gate WelcomeBack with `!showFirstTimeFlow` so first-time users never see both.
- For Electron-only projects with UI acceptance criteria, "verify in browser" in a PRD is aspirational unless the project has an explicit web-renderer mode. Document when skipped — don't silently claim browser verification that didn't happen.
---

## 2026-04-21 19:55 PDT - US-006
- Added IPC handlers in `src/main/main.ts`: `db:getSnoozedTasks`, `db:snoozeTask`, `db:bringBackTask`, `db:bringBackAllTasks` (the last iterates snoozed tasks and calls `bringBackTask` per id, returning `{ count }`). Imported `getSnoozedTasks`, `snoozeTask`, `bringBackTask` from database.ts.
- Exposed wrappers in `src/main/preload.ts`: `getSnoozedTasks`, `snoozeTask(id, reason)`, `bringBackTask(id)`, `bringBackAllTasks()` — all invoke-style (no `allowed[]` push-channel update needed).
- Mirrored wrappers in `src/renderer/api.ts` so components use `api.getSnoozedTasks()` / `api.bringBackTask(id)` / `api.bringBackAllTasks()`.
- Extended `src/renderer/MyTasks.tsx`:
  - Added `SnoozedTask` type alias + `SnoozedRow` sub-component (title, due date, relative `snoozedAt` via local `formatRelative`, human-readable `snoozedReason` via `humanSnoozedReason` — 'stale-30d' → 'auto-snoozed — no activity for 30+ days').
  - New `filter` state (`'board' | 'snoozed'`) with pill-style buttons at the top of the dashboard. Snoozed pill shows a count badge when snoozedTasks.length > 0 (hidden otherwise per PRD).
  - Fetches snoozed tasks on mount (for the badge) and re-fetches whenever entering the Snoozed view.
  - `handleBringBack` removes the row optimistically, fires a 3-second success toast ('Brought back — back in your active list.'), and re-fetches active tasks so the brought-back task shows up on the board.
  - `handleBringBackAll` calls the new IPC, empties the local list, toasts `Brought back N task(s)`, and re-fetches active.
  - Empty-state: friendly copy explaining that the staleness sweep will populate this view.
  - Kanban board and pending-approval banner are wrapped in `filter === 'board'` so they hide when Snoozed is active.

Files changed:
- src/main/main.ts (imports + 4 new IPC handlers)
- src/main/preload.ts (4 new invoke wrappers)
- src/renderer/api.ts (4 new method wrappers)
- src/renderer/MyTasks.tsx (type + row + filter-pill UI + handlers + conditional rendering)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `npx webpack --config webpack.renderer.config.js` — bundle compiled successfully.
- `node dist/main/{config,database,staleness-sweep,welcome-back,self-identity}.test.js` — regression green.

Browser verification:
- **Dev-browser skill was NOT available in this session** (same as US-005). Verification relied on: (a) main + renderer typecheck clean, (b) full webpack bundle passes, (c) structural inspection of JSX against PRD copy spec, (d) unit-test regression across all 5 pre-existing suites. Recommended follow-up human smoke: launch Electron, seed 3 snoozed tasks via `snoozeTask(id, 'stale-30d')` in the main-process console, navigate to Tasks → Snoozed, confirm rows render with reason copy and relative `snoozedAt`, click [Bring back] on one and [Bring back all] on the rest.

Learnings for future iterations:
- The existing `MyTasks.tsx` had NO pre-existing filter-pill system — tasks render as a 3-column kanban. PRD wording "alongside existing filters" was aspirational; the pragmatic interpretation is to ADD pill-style toggles at the top that switch the entire view mode (Board ↔ Snoozed). Future stories that add more list-style filters (e.g., US-007 likely-done pill) can reuse this pattern.
- When adding a new IPC channel that just wraps an existing DB helper, the boilerplate spans 3 files: `main.ts` (handler), `preload.ts` (invoke wrapper), `api.ts` (typed method). Keep the 3 in lockstep — if any one is missing, the renderer gets `undefined is not a function` at runtime. None of these need entry in the `allowed[]` allowlist (that list is ONLY for `ipcRenderer.on` push channels — reiterated from US-004/US-005).
- `bringBackAllTasks` on the main side iterates snoozed tasks and calls `bringBackTask` per id. A future optimisation could batch this as a single DB update if performance matters (NeDB doesn't support `$set` across many docs as atomically, so the per-doc loop is fine for now).
- Relative-time formatting: I wrote a local `formatRelative(iso)` rather than pulling in a date-fns-style dep. The codebase already inlines `toLocaleDateString()` for absolute dates (see existing TaskCard) — local helper matches that style.
- `handleBringBack` calls `fetchTasks()` after every bring-back so that when the user flips back to the Board view, the restored task is already visible. Otherwise the user would see stale state until the next render-refresh.
- Badge count on the Snoozed pill: fetching `getSnoozedTasks` once on mount keeps it live for the badge even before the user clicks the pill. If a later story adds real-time staleness-sweep events from main, wire a push channel and call `fetchSnoozedTasks` in the handler (mirror the `tasks:reprioritized` pattern).
---
