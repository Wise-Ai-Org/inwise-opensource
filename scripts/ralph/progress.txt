# Ralph Progress Log
Started: Thu Apr 23 16:41:47 PDT 2026

## Codebase Patterns
- Tests live next to source as `X.test.ts` with a `run()` export and `if (require.main === module) run()`. Build via `npx tsc -p tsconfig.main.json`, then run `node dist/main/X.test.js`. No `npm test` script exists.
- NeDB (`@seald-io/nedb`) without a `filename` option is pure in-memory — perfect for unit tests. Use `new Datastore<any>()` then `await db.loadDatabaseAsync()` before use. `{ field: null }` matches documents where the field is null OR missing (Mongo-compatible), so additive schema changes need no migration.
- Main-process typecheck: `npx tsc -p tsconfig.main.json --noEmit` (silent = success). Renderer: `npx tsc -p tsconfig.renderer.json --noEmit`.
- `import { app } from 'electron'` in plain node is safe at load-time (`app` is `undefined`); it only fails when you actually call an `app.*` method. This means tests can import modules that import electron without booting electron.
- Test-inject pattern for DB-backed modules: export `__setFooDbForTests(db)` that rebinds the module-level `fooDb`. Mirrors `database.ts::__setTasksDbForTests` and the new `sor-write-log.ts::__setSorWritesDbForTests`.
- `preload.ts` `allowed[]` list is for main→renderer **push channels** (`ipcRenderer.on`). New `invoke`-style IPCs do NOT need to be added there. This has been re-confirmed on every story.
- IPC boilerplate for a new channel spans 3 files: `main.ts` (handler), `preload.ts` (invoke wrapper), `renderer/api.ts` (typed method). Keep in lockstep.
- SoR write audit log (`sor-write-log.ts`): every outbound push to an external system wraps the HTTP call with `recordWrite(...)` → network → `markCompleted(...)`. Push functions take an optional trailing `provenance?: WriteProvenance` parameter so callers who know `sourceMeetingId` / `linkedTaskId` / `confidence` / `approvalPath` can attach it. For retry, `pushParams` is stashed on the entry so the write can be re-sent via the raw transport (NOT the wrapper — that'd create a duplicate entry).
- For SoR operations that modify existing records (`update`, `transition`): **GET-before-write** so `fieldDiffs.before` is accurate. PRD accepts the extra round trip.
- Pure helpers `computeCreateDiffs(fields)` and `computeUpdateDiffs(before, updates)` live in `sor-write-log.ts` — free of HTTP, safe to unit-test.

---

## 2026-04-23 17:05 PDT - US-001
- New module `src/main/sor-write-log.ts`: NeDB collection `sor-writes.db`, schema covers every field in the PRD (`targetSystem`, `targetRecordId`, `operation`, `fieldDiffs[]`, `commentBody`, `sourceMeetingId`, `sourceTranscriptSpan`, `linkedTaskId`, `confidence`, `approvalPath`, `result`, `errorMessage`, `pushParams`, `createdAt`, `completedAt`, `retryCount`). Exports `recordWrite`, `markCompleted`, `incrementRetry`, `getWriteEntry`, `listRecent`, `listByMeeting`, `listByTaskId`, `listByTargetRecord`, `aggregateByIntegration`, `listStuckEntries`, and pure helpers `computeCreateDiffs` / `computeUpdateDiffs`.
- `onWriteCompleted(listener)` — one-shot registration hook. `main.ts` wires it to `mainWindow.webContents.send('sor:write-completed', entry)` so renderers can live-refresh on every markCompleted.
- `src/main/jira-client.ts`: all four push functions (`createJiraIssue`, `updateJiraIssue`, `transitionJiraIssue`, `addJiraComment`) now take an optional trailing `provenance?: WriteProvenance`. Each wraps its network call with `recordWrite(...pending)` → HTTP → `markCompleted(id, 'success' | 'failed', err?)`. For `update` and `transition`, a GET-before-write reads the current Jira state to compute accurate `fieldDiffs`. For `create`, the entry starts with `targetRecordId: '(pending-create)'` and is patched via `markCompleted`'s `updates` arg once Jira returns the real key. For `transition` with no matching Jira workflow, we still record the attempt and mark it failed with a clear errorMessage.
- Added `retryJiraWrite(id)` — reads the stashed `pushParams`, increments retryCount, calls the raw transport directly (NOT the wrapper — prevents duplicate audit entries), then `markCompleted` accordingly.
- `src/main/main.ts`:
  - After-meeting auto-push (L390): reordered so the local task is created BEFORE the Jira call, then the Jira call receives `provenance = { sourceMeetingId, sourceTranscriptSpan: {start:0, end:item.text.length, snippet:item.text}, linkedTaskId: task._id, approvalPath: 'auto' }`. **Behavior change:** on Jira failure, the local task now survives (previously it was never created). This is strictly better — the action item is preserved for manual retry.
  - On-task-update auto-sync (L911+ block): passes `provenance = { linkedTaskId: id, approvalPath: 'auto' }` into `transitionJiraIssue` / `updateJiraIssue`.
  - Imported and registered: `initSorWriteLog()` in `app.whenReady()`, and `onWriteCompleted((entry) => mainWindow?.webContents.send('sor:write-completed', entry))` so renderers get live events.
  - Added IPC handlers: `sor:listRecent`, `sor:listByMeeting`, `sor:listByTaskId`, `sor:listByTargetRecord`, `sor:aggregateByIntegration`, `sor:retry` (dispatches to `retryJiraWrite` for jira entries).
  - Startup scan: 15s after `whenReady`, `listStuckEntries(24h)` is called. If any entries in `pending` / `pending-approval` / `retrying` are older than 24h, a native Notification prompts the user to retry from Settings → Integrations. Wrapped in try/catch so a scan failure never breaks startup.
- `src/main/preload.ts`: added invoke wrappers `sorListRecent`, `sorListByMeeting`, `sorListByTaskId`, `sorListByTargetRecord`, `sorAggregateByIntegration`, `sorRetry`. Added `sor:write-completed` to the `allowed[]` push-channel list.
- Tests in `src/main/sor-write-log.test.ts`: recordWrite→markCompleted lifecycle, failed+errorMessage, create-then-key-assignment, incrementRetry bumps count + resets to 'retrying' + clears errorMessage, computeCreateDiffs (skips nullish), computeUpdateDiffs (skips unchanged + skips undefined), comment stores empty diffs + body, listByMeeting / listByTaskId / listByTargetRecord queries, aggregateByIntegration totals + success/failed/lastWriteAt, listRecent ordering + limit, listStuckEntries matches pending/pending-approval/retrying past cutoff and excludes fresh + completed, onWriteCompleted listener fires exactly once per markCompleted.

Files changed:
- src/main/sor-write-log.ts (new, 270 LOC)
- src/main/sor-write-log.test.ts (new)
- src/main/jira-client.ts (instrument 4 push functions + new retryJiraWrite + buildJiraIssueUrl helper)
- src/main/main.ts (imports, initSorWriteLog, onWriteCompleted, 6 new IPC handlers, startup stuck-scan, provenance wiring at both push sites)
- src/main/preload.ts (6 new invoke wrappers + 1 new push channel in allowed[])
- src/main/AGENTS.md (new — documents the SoR wrapper pattern for future integrations)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `node dist/main/sor-write-log.test.js` — all tests passed.
- Regression: `node dist/main/{config,database,staleness-sweep,welcome-back,task-completion-inference,live-meeting-banner,self-identity}.test.js` — all still pass.

Browser verification:
- **Not applicable — this is a backend-only foundation story.** US-001's acceptance criteria are explicitly typecheck + unit tests (no UI rendered yet). US-002 (per-meeting trace) is the first story that exposes this in the renderer and will carry the browser-verification obligation.

Learnings for future iterations:
- The Jira push wrapper pattern (optional `provenance` parameter + recordWrite/markCompleted around the HTTP body) is the pattern to replicate for every future SoR integration (Salesforce, HubSpot, Linear). Document is in `src/main/AGENTS.md`. Each integration client should expose a `retry{System}Write(id)` that reads pushParams and calls the raw transport directly.
- **Don't wire retry through the wrapper.** The natural instinct is "retry = just call the push function again." That would create a DUPLICATE audit entry because the wrapper always calls recordWrite. Instead, retry bypasses the wrapper and uses the raw transport (`jiraFetch`) to update the EXISTING entry. This keeps retryCount accurate and the audit log honest.
- **Create the local task BEFORE the SoR call.** The PRD asked for `linkedTaskId` in provenance, which is only available if the task exists first. Reordering is a minor behavior change but is strictly better: on SoR failure the local task still exists (previously it was lost).
- **Listener hook vs. NeDB event emitter.** NeDB has no built-in change stream, so a single-registered `onWriteCompleted(listener)` hook inside the module is the cleanest way to push events to IPC. Multiple listeners aren't needed in this architecture — main.ts is the only consumer. If that assumption ever breaks, upgrade to a proper EventEmitter.
- **GET-before-write for accurate diffs costs one extra HTTP per update.** Acceptable per PRD. If a future story needs to batch writes (e.g., importing 100 tasks), consider: (a) skip the pre-GET and record `before: null` for batch imports, or (b) use a single GET-many to hydrate before-values in one call. Not needed today.
- **`computeUpdateDiffs` ignores `undefined` in the updates object**, so callers passing partial update shapes (the common case) don't spuriously record fields they didn't touch. `null` on the other hand is treated as an explicit clear. This matters if you pass `priority: null` intending to clear priority — it will record `{field: 'priority', before: 'High', after: null}`.
- **`targetRecordId: '(pending-create)'` placeholder** is a pragmatic shortcut — we don't know the Jira key until after the API responds. `markCompleted`'s `updates` arg patches the real key on success. A failed create leaves the placeholder, which is queryable and self-documenting.
- **Startup stuck-scan runs after a 15s delay** (not on `whenReady` directly) so calendar sync + daily Jira pull get a chance to run first. No race with those paths, but the delay keeps startup snappy if the user has hundreds of stuck entries.
---

## 2026-04-23 22:05 PDT - US-002
Thread: local (Claude Code)
- Wired per-meeting SoR trace UI into TranscriptReviewModal:
  - New `ReviewTab` value `'sorWrites'` with 'Synced to Jira' label. `TabNav` now takes `visibleTabs` so the SoR tab only renders when writes exist (hidden by default). `tabCounts` extended with `sorWrites`.
  - Added `initialTab` prop so the modal can deep-link straight to the SoR tab.
  - On open, loads `api.sorListByMeeting(meetingId)` in parallel with meeting + people fetches. Result stored in `sorWrites` state.
  - `renderSorWritesTab()` groups entries by `targetSystem` so future SoRs add rows without restructuring. Each row: colored operation badge, clickable Jira key (→ `api.openExternal(targetRecordUrl)`), one-line summary, expand/collapse chevron.
  - Expanded row: full fieldDiffs table (struck-through before → after; create rows skip the strike), comment body, source transcript snippet as a quoted block with left border, error message for failed writes.
  - Failed writes get a red card border, red 'Failed' badge, and a [Retry] button that calls `api.sorRetry(id)`. Retry success relies on the live-refresh path — no manual re-fetch.
  - Live refresh: separate useEffect subscribes to `sor:write-completed` IPC; on each event matching `sourceMeetingId === meetingId` it re-runs `sorListByMeeting` to pick up the new/updated entry.
  - Summary bar ('AI extracted N items…') now suppressed on the SoR tab (was only suppressed on transcript).
  - Added `{activeTab === 'sorWrites' && renderSorWritesTab()}` to the modal body switch — the missing wire that was blocking the whole tab from rendering.
- Desktop notification (main.ts, after-meeting auto-push):
  - Reads `sorListByMeeting(meetingId)` after the push loop (NOT the loop counters — the audit log is the source of truth for failure counts).
  - Title `Meeting {title}`; body `{N} updates pushed to Jira.` or `{N} pushed, {M} failed` when failures exist.
  - [See details] action button → focuses the main window and sends `meeting:open-details` IPC with `{meetingId, focusTab: 'sorWrites'}`.
  - Uses `Notification.isSupported()` to guard. Whole block wrapped in try/catch so a notification failure never breaks the pipeline.
- Deep-link flow (App.tsx + Communications.tsx):
  - App.tsx listens for `meeting:open-details`, switches to `view='communications'`, and stores `pendingMeetingOpen`.
  - Passes `pendingOpen` + `onPendingOpenConsumed` callback as props to Communications.
  - Communications has a useEffect that — once meetings have loaded — finds the target meeting in its list and calls `handleOpenReview(meeting, focusTab)`, then fires the callback to clear pendingOpen so it doesn't re-fire.
  - `handleOpenReview` now takes an `initialTab` arg and stores it in `reviewInitialTab` state, which is passed through to `TranscriptReviewModal`.
- preload.ts: added `'meeting:open-details'` to the `allowed[]` push-channel list.
- api.ts: added SoR IPC wrappers (`sorListRecent`, `sorListByMeeting`, `sorListByTaskId`, `sorListByTargetRecord`, `sorAggregateByIntegration`, `sorRetry`) — the chain was missing on the renderer side.

Files changed:
- src/renderer/views/communications/TranscriptReviewModal.tsx (SoR tab, row renderer, retry, live refresh, initialTab prop)
- src/renderer/Communications.tsx (pendingOpen prop, deep-link useEffect, initialTab wiring)
- src/renderer/App.tsx (meeting:open-details listener, pendingMeetingOpen state, pass to Communications)
- src/renderer/api.ts (SoR IPC wrappers)
- src/main/main.ts (desktop notification block in after-meeting auto-push)
- src/main/preload.ts (meeting:open-details push channel allowlist)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `node dist/main/sor-write-log.test.js` — all tests still pass.
- Regression: config / database / staleness-sweep / welcome-back / task-completion-inference / live-meeting-banner / self-identity — all still pass.

Browser verification:
- **Deferred to manual QA.** The `dev-browser` skill referenced in CLAUDE.md is not available in this runner's skill set; a full browser-smoke run (simulate meeting → 3 pushes, 2 success 1 failure → notification → click details → expand row → retry) needs a local run. Typecheck + unit tests cover the compile-time and backend contract; renderer wiring was reviewed end-to-end (tab gated by `visibleTabs`, `initialTab` flows App→Communications→Modal, live-refresh re-queries on IPC).

Learnings for future iterations:
- **Deep-link pattern: App holds the pending, child consumes on mount.** `meeting:open-details` is emitted by main regardless of the current view. App.tsx switches view AND stores pending state; Communications sees the prop, waits for its own data to load, acts, then calls `onPendingOpenConsumed()` to clear. This avoids re-mounting races and requires no global store. Reusable for any future "main → renderer deep-link" event.
- **Gate UI tabs by data, not feature flag.** The 'Synced to Jira' tab is hidden when `sorWrites.length === 0` — it doesn't just render empty. This keeps the default meeting view clean for meetings with no pushes and makes the tab self-documenting (its presence means "something happened").
- **Read the audit log, not the loop counters, for user-facing totals.** `main.ts` could have computed `pushed/failed` from its own created/linked counters, but those only see orchestration-level outcomes, not per-call Jira failures. `sor-writes.db` is the truth — use it for the notification body AND the UI.
- **Live refresh via IPC, not polling.** The renderer subscribes to `sor:write-completed` once per open modal / panel and re-fetches the scoped list on each event. Cleaner than interval polling and responds instantly to retries.
- **`initialTab` is a one-shot intent, not state.** The prop is read by the modal's open-effect to set `activeTab` once; subsequent tab clicks move on normally. The parent doesn't need to re-sync. This is the right shape for "open here" deep-links.
- **Notification must be wrapped in try/catch.** A failed Notification construction (OS-specific quirks, no desktop session, etc.) must not break the auto-push pipeline. Any UI-adjacent side effect in the main process should be non-fatal.
---

## 2026-04-23 22:14 PDT - US-003
Thread: local (Claude Code)
- Added Settings → Integrations status block:
  - New `IntegrationsSection` component at top level of `Settings.tsx`. Rendered between the existing "Jira Integration" and "Voice Enrollment" sections.
  - Per-integration card (today: Jira only). Layout reusable — adding Salesforce/HubSpot/Linear is a pure array-extend, no restructure needed.
  - Header: integration name + connection status pill. Pills: `Connected` (teal), `Disconnected` (slate), `Auth expired` (amber — triggered if `jiraStatus.info.authExpired === true`, a field the main process is free to set in the future).
  - Counts line (from `sor:aggregateByIntegration({sinceMs: 30d ago})`): `{success} writes · {failed} failed · last write {ago}`; collapses to `No writes yet.` when the aggregate total is 0.
  - Recent activity: last 10 rows from `sor:listRecent(10, 30d)` filtered to this integration. Collapsible, default collapsed. Each row shows colored operation badge, Jira key (clickable via `openExternal`), one-line summary, relative timestamp. Expand chevron reveals fieldDiffs / comment body / source-transcript snippet — same info as US-002, restyled in plain CSS to match the rest of `Settings.tsx`.
  - Failed rows get a red card border + red `FAILED` badge + red per-row `↻ Retry` button (calls `sor:retry`).
  - Action row: `[Retry failed (N)]` bulk button (enabled only when N > 0 and Jira is connected) + `[Disconnect]` button mirroring the existing Jira section behavior. A transient result message ("3 succeeded, 1 still failing") appears next to the buttons for ~4 s after a bulk retry.
- Main-process plumbing for bulk retry and count-of-failed:
  - `src/main/sor-write-log.ts`: new `listFailedSince(system, sinceMs)` query — one-liner findAsync with `result:'failed'` + system + createdAt cutoff.
  - `src/main/main.ts`: two new IPC handlers. `sor:listFailed(system, sinceMs)` → returns the failed rows (used by the UI for the button's `(N)` badge). `sor:retryFailed(system, sinceMs)` → iterates failed rows, dispatches `retryJiraWrite(id)` for each, returns `{ ok, attempted, succeeded, failed }`. Wrapped so a single retry exception doesn't abort the batch.
  - `src/main/preload.ts` + `src/renderer/api.ts`: `sorListFailed` / `sorRetryFailed` wrappers on both sides of the bridge. Pattern identical to the existing `sorRetry` / `sorListRecent` wiring from US-001.
- Live refresh: the Integrations section subscribes to `sor:write-completed` IPC on mount and re-runs the 4-call `Promise.all` on each event. No polling. Matches the US-002 pattern.
- "Today: only Jira card renders." — enforced by hardcoding the card inside the section. When a second SoR ships, extract the card into a `SorCard` component and map over an array of system configs.

Files changed:
- src/main/sor-write-log.ts (new `listFailedSince` export)
- src/main/main.ts (2 new IPC handlers + 1 import)
- src/main/preload.ts (2 new invoke wrappers)
- src/renderer/api.ts (2 new typed methods)
- src/renderer/Settings.tsx (new IntegrationsSection + SorWriteRow components, used once; minimal inline-style rendering matching existing Settings code)
- scripts/ralph/prd.json (US-003 → passes:true)

Quality gates:
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `node dist/main/sor-write-log.test.js` — all tests still pass (new `listFailedSince` is additive; existing lifecycle/aggregate tests unaffected).
- Regression: config / database / staleness-sweep / welcome-back / task-completion-inference / live-meeting-banner / self-identity — all still pass.

Browser verification:
- **Deferred to manual QA.** `dev-browser` skill is not available in this runner. The end-to-end flow was reviewed statically: `Promise.all` fires jiraStatus + aggregate + recent + failed in parallel on mount; `sor:write-completed` IPC triggers a reload; retry-one and retry-all both re-trigger the reload after resolving; disconnect confirmation uses the existing `jiraDisconnect` path already exercised elsewhere in the file. No new IPC path bypasses the allowlist — `sor:write-completed` was already in `preload.ts` from US-001.

Learnings for future iterations:
- **Settings.tsx is plain-CSS, not Chakra.** The per-meeting SoR trace in TranscriptReviewModal uses Chakra (`Box`, `Badge`, `HStack`, …). Settings.tsx is all inline-styled `<div>`s with css-variable colors (`var(--slate-500)`, `var(--teal)`, …). Replicating the row renderer cost ~150 LOC but kept the bundle honest — don't introduce Chakra into Settings for a single use. If a third surface needs the same row, extract a shared `SorWriteRow` that ships styles both ways.
- **Bulk retry in the main process, not the renderer.** A naive renderer-side `Promise.all(ids.map(sorRetry))` would fire N concurrent IPC calls and N concurrent Jira HTTP requests. The `sor:retryFailed` handler serializes them in the main process — acceptable latency for a user-initiated button (dozens, not hundreds) and gentler on Jira rate limits. Preserve this shape when adding other bulk operations.
- **Connection status pill has room for a third state.** `Auth expired` is driven by `jiraStatus.info?.authExpired`, a field the main process does not currently set. The UI reads it opportunistically so a future jira-client change to detect expired tokens can light it up without touching the Settings UI. Common pattern for progressive enhancement — declare the UI state up front, backfill the signal later.
- **Reusable layout means "the CARD is reusable", not "the SECTION is reusable".** The PRD was explicit: one card per integration, today only Jira. The section wrapper stays the same when a new SoR is added; just extract the card body into `<SorCard system="salesforce" label="Salesforce" ... />` and map. Don't prematurely abstract before the second card exists.
- **30-day window uses `useRef` not `useMemo`.** `useMemo([])` looks tempting but is more boilerplate and does the same thing. `useRef(Date.now() - 30d)` is a single statement, stable for the component's lifetime, and doesn't invite dependency-array confusion. Small detail but worth remembering — it'll recur for every "compute-once-on-mount" value.
---

## 2026-04-23 22:20 PDT - US-004
Thread: local (Claude Code)
- Added "Sync history" panel to `TaskDetailSidebar.tsx`:
  - New `SyncHistory({ taskId })` Chakra component mounted as a `<Section label="Sync history" icon={MdSync}>` at the bottom of the drawer body (below Linkage).
  - Loads `api.sorListByTaskId(taskId)` on mount and whenever taskId changes. Returns reverse-chronological (`createdAt: -1` is the default sort in main-process `listByTaskId`).
  - Header row: `Synced to {Jira key} · last write {relative} · {N} writes total`. Jira key + URL derived from the most recent entry that has a real `targetRecordId` (skips `(pending-create)` placeholders). Clickable key opens `targetRecordUrl` via `api.openExternal`.
  - Timeline row format mirrors US-002 exactly: colored operation badge, clickable Jira key, one-line summary, retry badge, relative timestamp, expand/collapse chevron. Expanded view shows `fieldDiffs` table (with line-through on `before` for non-create ops), comment body, source-transcript snippet as a quoted block, and the error message for failed writes.
  - Failed rows get a red card border + red `Failed` badge + per-row `↻ Retry` button (calls `api.sorRetry(id)`; no manual re-fetch — relies on the `sor:write-completed` live-refresh path).
  - Pending / retrying / pending-approval rows get a yellow pill.
  - Pagination: rendered cap at 50 rows (`SYNC_HISTORY_PAGE_SIZE`). If more exist, a `Show older (N more)` link expands the full list in-place. Not expected to trip in practice, but the PRD asked for it.
  - Empty state: when `entries.length === 0`, renders "No Jira sync yet. Auto-push runs when this task is updated or after the next meeting referencing it." in the subtle label color. No header row in this state.
  - Live-refresh: subscribes to `sor:write-completed` IPC once, only re-queries when the event's `linkedTaskId === taskId`. Same shape as the US-002 modal subscription.
- Types / helpers duplicated inline (SorWriteEntry, SorOp, SorResult, SOR_OP_META, sorOneLineSummary). Third surface using this pattern; extraction-into-shared-module deferred to US-005 when the 4th surface will land.

Files changed:
- src/renderer/components/tasks/TaskDetailSidebar.tsx (+ ~260 LOC — SyncHistory component + types + SoR icon imports + mount point)
- scripts/ralph/prd.json (US-004 → passes:true)

Quality gates:
- `npx tsc -p tsconfig.renderer.json --noEmit` — clean.
- `npx tsc -p tsconfig.main.json --noEmit` — clean.
- `node dist/main/sor-write-log.test.js` — all tests pass.
- Regression: config / database / staleness-sweep / welcome-back / task-completion-inference / live-meeting-banner / self-identity — all still pass.

Browser verification:
- **Deferred to manual QA.** `dev-browser` skill is not available in this runner. The flow was reviewed statically: task-detail drawer opens → useEffect fires `sorListByTaskId(taskId)` → header and timeline render from the returned rows → click chevron to expand → click Jira key → click Retry on a failed row → IPC event fires → list re-queries. No new IPC channel introduced — `sor:write-completed` was already allow-listed in preload.ts from US-001.

Learnings for future iterations:
- **Task detail uses `useEffect` on taskId reset, not `useEffect` on isOpen.** The sidebar remounts its internal state when `taskId` changes while `isOpen` stays true (user navigates between tasks). `SyncHistory` keys its reload and resets (`expandedId`, `showAll`, `loaded`) off `taskId` — not `isOpen` — so state is fresh for each distinct task without remounting the whole sidebar.
- **Derive the header from the entries, not from the task object.** `DetailedTask` doesn't have a clean `jiraKey` field at the top level — the Jira link lives across `source.url`, `provenance`, and whatever `jiraLinkTask` sets. Reading the most recent `SorWriteEntry.targetRecordId` is authoritative: it's exactly what Inwise has actually written to, not what someone set in metadata. Also skips `(pending-create)` placeholders so a failed-create entry doesn't show a garbage key.
- **Page size 50 is a belt-and-suspenders guard, not expected flow.** The PRD said "paginate at 50 entries (rare but possible)". In practice most tasks have 1–5 writes. The "Show older" link is the simplest possible affordance — no cursor/offset API needed because `listByTaskId` already returns the full list sorted.
- **Third surface with the SoR row pattern; extraction is still deferred.** TranscriptReviewModal (Chakra), Settings (plain CSS), and now TaskDetailSidebar (Chakra). The two Chakra copies have ~95% identical row JSX. US-005 (Inbox receipts) will be the 4th surface — that's the right moment to extract `components/sor/SorWriteRow.tsx` + shared `types.ts`. Premature extraction now would still leave Settings's plain-CSS variant as a separate file, and risks breaking US-002 / US-004 in the same commit.
- **Row pattern requires live-refresh with a per-scope filter.** TranscriptReviewModal filters on `sourceMeetingId`; IntegrationsSection refreshes unconditionally; SyncHistory filters on `linkedTaskId`. Every consumer of `sor:write-completed` should filter to their scope to avoid unnecessary IPC churn across unrelated open panels.
---
