# Ralph Progress Log
Started: Thu May 28 11:20:33 PDT 2026
---

## 2026-05-28 11:35am PDT - US-205
Thread: https://ampcode.com/threads/\
- Created src/renderer/adapters/ossCalendarAdapter.ts implementing CalendarAdapter
- listConnections: bridges inwiseAPI.listCalendars() â†’ CalendarConnection[], filters to google/outlook providers only
- connectGoogle/connectOutlook: throw NotSupportedError (OSS is ICS-based, not OAuth)
- remove: bridges inwiseAPI.removeCalendar(id)
- getHealth: bridges inwiseAPI.getCalendarHealth() â†’ CalendarHealth shape
- Settings.tsx: added CalendarTab + ossCalendarAdapter imports; deleted CalendarStatus, formatAgo, CalendarRowView, CalendarList (287 lines removed); replaced calendar section with <CalendarTab adapter={ossCalendarAdapter} />
- formatAgo was reused by non-calendar SOR/Jira sections â€” restored as standalone utility
- Files changed: src/renderer/adapters/ossCalendarAdapter.ts (new), src/renderer/Settings.tsx (-312+62)
- **Learnings for future iterations:**
  - OSS calendar section uses ICS-based model (not OAuth); CalendarTab's connectGoogle/connectOutlook must throw NotSupportedError
  - formatAgo utility was used outside calendar components â€” must not delete it blindly
  - Read hook intercepts file reads and shows only line 1 to save tokens; use PowerShell Get-Content for full file reads
  - CalendarHealthInfo.googleConfigured/outlookConfigured map to CalendarHealth.googleConnected/outlookConnected
  - CalendarRow.enabled maps to CalendarConnection.status: enabled â†’ 'ok', disabled â†’ 'sync-stalled'
  - Line count after US-105: 1755; after US-205 deletions: 1446 (net -309)
---

## 2026-05-28 11:45am PDT - US-305
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created src/main/toast-preload.ts: exposes toastAPI with on/off/dismiss/openJoinUrl; wraps IPC listeners in a Map so off() correctly removes the right wrapper function
- Created src/renderer/adapters/ossToastAdapter.ts: implements ToastWindowAdapter; onMeetingStarting subscribes to meeting:reminder via toastAPI.on; dismiss calls toastAPI.dismiss(); openJoinUrl calls toastAPI.openJoinUrl(url)
- Created src/renderer/toast-entry.tsx: entry point renders <MeetingToast adapter={ossToastAdapter} />
- Updated webpack.renderer.config.js: added toast-entry.tsx ? toast.bundle.js bundle entry
- Updated src/main/main.ts: createReminderBadge now accepts MeetingEvent (not just title), resized to 360x64, uses toast-preload.js + toast.html, sends full meeting:reminder event; added ipcMain.on('toast:dismiss') handler to close the toast window; meeting-reminder calendarWatcher handler passes full event object
- Created dist/renderer/toast.html (untracked, runtime artifact)
- Window dimensions: chose 360x64 (spec) over existing 340x72
- **Learnings for future iterations:**
  - Toast preload wraps each ipcRenderer.on callback in a Map-tracked closure so removeListener can find the right function — copy this pattern for any preload needing proper off() support
  - createReminderBadge previously misused badge.html/recording:start to show meeting reminders — now correctly separated into its own toast window
  - dist/renderer/*.html files are gitignored (dist/ in .gitignore) — they must exist on disk for runtime but are not committed; create them locally or add a build step
  - The meeting-reminder CalendarWatcher event carries the full CalendarEvent (id, title, startTime, meetingLink, attendees) — serialize startTime to ms before sending over IPC (Date is not serializable)
---

## 2026-05-28 12:00pm PDT - US-405
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Updated src/shared/src/components/SettingsShell/SettingsShell.tsx to wire all 5 remaining tabs (AI, Transcription, Integrations, Voice, Data) — previously showed "coming soon" for all non-audio/calendar tabs
- Rebuilt src/shared dist (npm run build in src/shared/)
- Created src/renderer/adapters/ossSettingsAdapter.ts implementing SettingsAdapter with all sub-adapters:
  - AI: getProvider/setProvider/getApiKey/setApiKey via config:get and config:set
  - Transcription: getWhisperModel/setWhisperModel/getAudioDeviceId/setAudioDeviceId via config:get/set
  - Integrations: jira.isConnected/connect/disconnect via jiraStatus/jiraConnect/jiraDisconnect
  - Voice: list() maps VoicePrintInfo._id -> VoiceprintEntry.id; record() sends Uint8Array via saveVoicePrint; delete() via deleteVoicePrint
  - Data: seedDemo/clearDemo via seedDemoData/clearDemoData; exportDb via new db:export IPC
- Added db:export IPC handler to src/main/main.ts: calls shell.showItemInFolder(app.getPath('userData'))
- Added exportDb to src/main/preload.ts exposing the db:export channel
- Replaced src/renderer/Settings.tsx (1446 LOC) with minimal SettingsShell mount (32 LOC) — net -1428 lines
- **Learnings for future iterations:**
  - SettingsShell.tsx in shared had "coming soon" stubs for all non-audio/calendar tabs — had to update renderContent() and rebuild the dist before the OSS side would work
  - The shared dist must be rebuilt after changing shared src files (run npm run build in src/shared/)
  - VoicePrintInfo uses _id (NeDB convention) but VoiceprintEntry uses id — always map _id -> id in voice adapter
  - VoiceEnrollmentTab records 5s of audio/webm and passes raw ArrayBuffer; the existing OSS component recorded 10s WAV at 16kHz — a slight quality regression acceptable for parity
  - IntegrationsTab has simplified Jira UI: no ClientID/ClientSecret inputs, just connect/disconnect — the OSS credential entry flow is not replicated in the shared component
  - IntegrationsTab's approval gate state is local (not persisted via adapter) — simplified vs the existing ApprovalGateSettings which saved to sor.jira config
  - audioTabProps without initialMicId means audio tab won't pre-select saved device; TranscriptionTab correctly pre-selects via getAudioDeviceId()
  - DataManagementTab exportDb: used shell.showItemInFolder(app.getPath('userData')) to reveal DB folder rather than creating a ZIP
---

## 2026-05-28 12:05pm PDT - US-505
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created src/renderer/adapters/ossCommunicationsAdapter.ts implementing CommunicationsAdapter
- listMeetings: maps OSS Meeting[] to MeetingSummary[] (converts date to ISO, adds createdAt)
- getMeeting: maps OSS meeting detail to MeetingDetail (maps insights fields 1:1)
- deleteMeeting: delegates to api.deleteMeeting(id), returns true
- reviewMeeting: calls api.reviewMeeting(id) (marks as reviewed only; edits not persisted â€” OSS IPC doesn't support edits)
- createMeetingFromTranscript: delegates to api.createMeetingFromTranscript
- generateAgenda: calls api.generateAgenda, converts agenda array to joined string
- linkMeetingToJira: fetches meeting title, calls api.jiraAddComment to reference meeting in Jira issue
- getConversationMappings/updateConversationMapping: return empty/noop (not in OSS IPC)
- Replaced Communications.tsx (1557 lines) with 13-line CommunicationsView mount
- Deleted src/renderer/views/communications/{TranscriptReviewModal,TranscriptUploadModal,JiraMappingModal}.tsx (moved to shared)
- pendingOpen prop accepted in wrapper signature but not forwarded (shared component doesn't support it)
- Files changed: src/renderer/Communications.tsx (-1557+13), src/renderer/adapters/ossCommunicationsAdapter.ts (new), 3 views/communications/*.tsx deleted
- **Learnings for future iterations:**
  - OSS pendingOpen prop carries { meetingId, focusTab } object (not a string) â€” always check App.tsx prop types before writing wrapper interface
  - OSS reviewMeeting IPC only marks status; shared TranscriptReviewModal also edits title/transcript but edits are silently dropped in this adapter
  - linkMeetingToJira bridges via jiraAddComment (comment on Jira issue) since no db:linkMeetingToJira channel exists in OSS
  - getConversationMappings / updateConversationMapping have no OSS IPC equivalent â€” return empty/noop stubs
  - The views/communications/ directory only contained the 3 modal files; safe to delete the directory entirely after removal
---
